import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.yuriimyronov.authenticator',
  appName: 'otp-authenticator',
  webDir: 'dist'
};

export default config;
